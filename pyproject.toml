[project]
name = "athlete-intelligence-platform"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "gunicorn>=23,<24",
  "psycopg[binary]>=3.2,<4",
]

[project.optional-dependencies]
test = []

[tool.setuptools.packages.find]
include = ["aip*"]
